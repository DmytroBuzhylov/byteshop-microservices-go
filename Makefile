
admin:
	cd AdminService && make run

auth:
	cd AuthService && make run

chat:
	cd ChatService && make run

order:
	cd OrderService && make run

payment:
	cd PaymentService && make run

product:
	cd ProductService && make run